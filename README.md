# receipt-extract

Receipt / invoice extraction pipeline: image or PDF -> vision LLM (tool-use enforced schema) -> Pydantic-validated structured data -> SQLite.

Status: scaffolding. Full documentation is added in the final phase.
